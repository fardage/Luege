#!/bin/bash
# Pre-commit hook: Run all tests before committing

echo "Running tests..."
make test-all

if [ $? -ne 0 ]; then
    echo ""
    echo "Tests failed. Commit aborted."
    echo "Fix the failing tests and try again."
    exit 1
fi

echo "All tests passed!"
exit 0
